Help for SET PASSWORD:

SET PASSWORD allows you to change the
password associated with a username or channel.
It is recomended that you change your passwords
on a regular basis.

Syntax: SET <username|#channel> PASSWORD <password>

Examples:
    /msg &nick& SET foo PASSWORD bar
    /msg &nick& SET #foo PASSWORD bar
